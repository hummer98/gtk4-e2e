{
  "$comment": "AUTO-GENERATED FROM packages/server/src/proto.rs — do not edit by hand",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "Capability": {
      "description": "Capability identifiers advertised in `Info.capabilities`.\n\nVariants are appended in the order in which they are surfaced. Step 6 extends the deterministic ordering to `[Info, Tap, Wait, Screenshot]`. Step 7 appends `Events` for the `WS /test/events` channel.",
      "enum": [
        "info",
        "tap",
        "wait",
        "screenshot",
        "events"
      ],
      "type": "string"
    }
  },
  "description": "Response payload of `GET /test/info`.",
  "properties": {
    "app_name": {
      "type": "string"
    },
    "app_version": {
      "type": "string"
    },
    "capabilities": {
      "items": {
        "$ref": "#/definitions/Capability"
      },
      "type": "array"
    },
    "instance_id": {
      "type": "string"
    },
    "pid": {
      "format": "uint32",
      "minimum": 0.0,
      "type": "integer"
    },
    "port": {
      "format": "uint16",
      "minimum": 0.0,
      "type": "integer"
    },
    "token_required": {
      "type": [
        "boolean",
        "null"
      ]
    }
  },
  "required": [
    "app_name",
    "app_version",
    "capabilities",
    "instance_id",
    "pid",
    "port"
  ],
  "title": "Info",
  "type": "object"
}
