{
  "$comment": "AUTO-GENERATED FROM packages/server/src/proto.rs — do not edit by hand",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Body of `POST /test/type` (Step 9).\n\nMVP semantics: the server replaces the entire text content of the resolved widget (Entry / Editable / TextView) with `text`. There is no \"insert at cursor\" mode — see plan §2.2.",
  "properties": {
    "selector": {
      "type": "string"
    },
    "text": {
      "type": "string"
    }
  },
  "required": [
    "selector",
    "text"
  ],
  "title": "TypeRequest",
  "type": "object"
}
