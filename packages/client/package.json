{
  "name": "gtk4-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Placeholder; real SDK lands in Step 4.",
  "scripts": {
    "lint": "exit 0",
    "fmt:check": "exit 0",
    "gen:types": "cargo run -p gtk4-e2e-server --example gen-schemas --features e2e && bun scripts/gen-types.ts"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15"
  }
}
